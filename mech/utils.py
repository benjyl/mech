g = 9.81
import numpy as np


def deg_to_rad(a):
    """
    Turns an angle in degrees into an angle in radians

    Args:
        a (float): Input in radians

    Returns: 
    """
    return a * np.pi / 180
